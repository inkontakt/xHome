# Copy to .env and set strong passwords before first start:
#   cp .env.example .env

WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-me-db-user
MYSQL_ROOT_PASSWORD=change-me-root
WP_PORT=8080
